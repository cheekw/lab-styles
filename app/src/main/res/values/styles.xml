<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="ThemeOverlay.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">#d9d9d9</item>
    </style>

    <style name="TextStyle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/uwPurple</item>
        <item name="android:textSize">22sp</item>
    </style>

    <style name="Label" parent="TextStyle">
        <item name="android:textStyle">bold</item>
        <item name="android:layout_marginEnd">@dimen/activity_horizontal_margin</item>
        <item name="android:layout_marginBottom">@dimen/activity_vertical_margin</item>
    </style>

    <style name="Gold" parent="Label">
        <item name="android:background">@color/uwGold</item>
        <item name="android:textColor">#FFFFFF</item>
    </style>

    <style name="Button" parent="android:MediaButton">
        <item name="android:textSize">22sp</item>
    </style>
</resources>
